import { HeaderContainer, HeaderText } from './styles';

export function Header() {
    return (
        <HeaderContainer>
            <HeaderText>Bem vindo ao estudo de lógica</HeaderText>
        </HeaderContainer>
    );
}